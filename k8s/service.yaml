apiVersion: v1
kind: Service
metadata:
  name: insurance-fe-service
  namespace: default
spec:
  selector:
    app: insurance-fe
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080