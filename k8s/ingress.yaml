apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: insurance-fe-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306275714-fe.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: insurance-fe-service
                port:
                  number: 80